{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "go.toml 配置",
  "description": "机器人运行时配置，字段含义见 config.go / go.toml.example。",
  "type": "object",
  "additionalProperties": false,
  "required": ["onebot", "openai", "groups"],
  "properties": {
    "onebot": {
      "type": "object",
      "title": "OneBot",
      "description": "与 OneBot 的连接相关设置。",
      "additionalProperties": false,
      "required": ["reverse_ws_url"],
      "properties": {
        "reverse_ws_url": {
          "type": "string",
          "minLength": 1,
          "title": "反向 WebSocket 地址",
          "description": "OneBot 反向 WebSocket 的 URL，例如 ws://127.0.0.1:3001。"
        }
      }
    },
    "openai": {
      "type": "object",
      "title": "OpenAI 兼容接口",
      "description": "调用大模型用的 OpenAI 兼容 HTTP API。",
      "additionalProperties": false,
      "required": ["base_url", "model"],
      "properties": {
        "base_url": {
          "type": "string",
          "minLength": 1,
          "format": "uri",
          "title": "API 基础地址",
          "description": "兼容 OpenAI 的 API 根 URL（不含具体 path 时以服务商文档为准）。"
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "title": "模型名称",
          "description": "对话补全使用的模型名。"
        }
      }
    },
    "groups": {
      "type": "array",
      "minItems": 1,
      "title": "群列表",
      "description": "按 QQ 群划分的机器人设置，对应 TOML 中的 [[groups]] 表。",
      "items": {
        "type": "object",
        "title": "单个群的配置",
        "additionalProperties": false,
        "required": ["qq", "prompt", "memory_file"],
        "properties": {
          "qq": {
            "type": "integer",
            "not": { "const": 0 },
            "title": "群号",
            "description": "QQ 群号，不能为 0。"
          },
          "prompt": {
            "type": "string",
            "minLength": 1,
            "title": "提示词",
            "description": "系统向提示词；可使用 Go text/template，常用变量：.SystemPrompt、.MemoryPrompt。"
          },
          "memory_file": {
            "type": "string",
            "minLength": 1,
            "title": "记忆文件路径",
            "description": "该群长期记忆提示词所在文件的路径。"
          }
        }
      }
    }
  }
}
